# build output
dist/
.output/

# dependencies
node_modules/

# logs
npm-debug.log*

# environment variables — NEVER commit secrets
.env
.env.local
.env.production

# OS
.DS_Store
Thumbs.db

# Astro cache
.astro/

# Cloudflare
.wrangler/

# Claude Code settings
.claude/

# Internal / sensitive files
brief.md
*.docx
*.pdf
"Untitled design.png"
founder.jpeg

# public/downloads is the site's download folder — ALL files there must be tracked
# (negation overrides the *.pdf / *.docx rules above)
!public/downloads/**

# Electron build artifacts and node_modules inside downloads — too large for GitHub (>100MB)
public/downloads/**/dist/
public/downloads/**/node_modules/

# Tool source files (sold products — not for public repo)
TOOL PRODUCTION PROJECT FOR DHA/

.dev.vars*
!.dev.vars.example
!.env.example
